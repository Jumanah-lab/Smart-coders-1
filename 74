import math

x = float(input("Enter x in radians: "))
n = int(input("Enter terms: "))

s = 0

for i in range(n):
    power = 2 * i + 1
    term = x ** power / math.factorial(power)

    if i % 2 == 0:
        s += term
    else:
        s -= term

print("Sum:", s)